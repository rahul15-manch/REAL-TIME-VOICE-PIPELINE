# ──────────────────────────────────────────────────────────────
# Real-Time Voice Pipeline — Dependencies
# ──────────────────────────────────────────────────────────────

# ── Pillar 1 core ──────────────────────────────────────────────
loguru>=0.7.0

# ── Pillar 2 — Real audio services (LiveKit + Daily + Deepgram + Groq + ElevenLabs) ──
pipecat-ai[livekit,silero,deepgram,elevenlabs,groq,rnnoise]==1.5.0
websockets>=11.0.0
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.9
twilio>=8.0.0
python-dotenv>=1.0.0
aiohttp>=3.9.0
deepgram-sdk>=3.0.0
groq>=0.9.0
livekit-api>=0.4.0
certifi>=2023.0.0
pytest==9.1.1
sqlalchemy>=2.0.0
asyncpg>=0.28.0
alembic>=1.12.0
greenlet>=3.0.0
pytest-asyncio>=0.21.0
aiosqlite>=0.19.0
